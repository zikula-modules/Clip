
  TODO

    EDITOR PANEL
      Adjustments to review the own publications + revisions

    ADMIN PANEL
      Update the pubtype title on the tree after update

    PLUGINS
      General:
        Expand the support to multiple rows with the introduction of D2
      Relations:
        Exclude already linked pubs?
      Text:
        Configurable cols, nl2br if scribite is not enabled?
      Category-based:
        Should validate if their categories are not in the current plugin tree

    DATA
      Pubdata models indexes
      New admin screen to validate the data integrity (only one online revision, title field, categories, etc)

    HOOKS
      Implement ui_hooks.form_delete
