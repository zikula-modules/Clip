
  CHECK
	Pagesetter code in pagemaster_adminapi_moveToDepot?

  TODO
	Maxlenght more clean solution. Exclude some default plugins?
	Move the publangs to l2 (0.4 upgrade routine, queries)
	modvar to enable template alerts
	rework the vars disposition in the templates (wrapped in proper arrays)
	rename the admin functions
	centralize include_once('includes/pnForm.php');
	deprecate PmWorkflowUtil
	migrate to the itemlist styles and clean the internal stylesheet
	category based plugins should validate if their categories are not in the current plugin tree
	integrate styles to workflow actions, like images and News buttons styles
